export const activeMenuIndex = (state) => state.activeMenuIndex;
export const searchKeyword = (state) => state.inputValue;
export const isVegOnly = (state) => state.vegOnly;
export const activeMenuItems = (state) => state.activeMenuItems;
export const cart = (state) => state.cart;
